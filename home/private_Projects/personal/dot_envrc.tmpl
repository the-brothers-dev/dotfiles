# ============================================================
# Personal 환경 설정
# direnv가 이 디렉토리 진입 시 자동 로드
# 활성화: direnv allow
# ============================================================

# Kubernetes
export KUBECONFIG="$HOME/Projects/personal/.kube/config"

# AWS
export AWS_CONFIG_FILE="$HOME/Projects/personal/.aws/config"
export AWS_SHARED_CREDENTIALS_FILE="$HOME/Projects/personal/.aws/credentials"
# export AWS_PROFILE="personal"

# Docker (필요시)
# export DOCKER_CONFIG="$HOME/Projects/personal/.docker"

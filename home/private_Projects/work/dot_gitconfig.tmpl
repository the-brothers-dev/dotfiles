# ============================================================
# Work Git 설정
# ~/Projects/work/ 디렉토리에서 자동 적용
# 회사별로 세분화하려면 work/company-a/.gitconfig 등 생성
# ============================================================
[user]
    name = {{ .name }}
    email = {{ .email }}

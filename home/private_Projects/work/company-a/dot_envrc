# ============================================================
# Company A 환경 설정
# direnv가 이 디렉토리 진입 시 자동 로드
# 활성화: direnv allow
# ============================================================

# Kubernetes
export KUBECONFIG="$HOME/Projects/work/company-a/.kube/config"

# AWS
export AWS_CONFIG_FILE="$HOME/Projects/work/company-a/.aws/config"
export AWS_SHARED_CREDENTIALS_FILE="$HOME/Projects/work/company-a/.aws/credentials"
export AWS_PROFILE="company-a-dev"

# Terraform (필요시)
# export TF_VAR_environment="dev"

# Docker Registry (필요시)
# export DOCKER_CONFIG="$HOME/Projects/work/company-a/.docker"
